minikube start
& minikube -p minikube docker-env --shell powershell | Invoke-Expression
docker build -t flyhr-app:1.0 .
kubectl apply -f deployment.yaml
kubectl get pods
kubectl get pods -A
kubectl get nodes
minikube service flyhr-service
kubectl get nodes
-->git init (it will initialize existing git repository)
-->git add .
--> git commit -m "first commit"
-->git branch -M main
-->git remote add origin https://github.com/Samhitha1705/minikube-basic.git
-->  Remove-Item -Recurse -Force .git (for force removing existing git repository)
-->git remote remove origin(for removing the origin)
